# Let us start simple! Given a pod-definition.yml file. 

# We are only getting started with it. 
# I have added two root level properties - apiVersion and kind.

# Instruction: Add the missing two properties - metadata and spec

# pod-definition.yml
apiVersion:

kind:

metadata:

spec:
